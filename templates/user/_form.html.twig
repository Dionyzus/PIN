{{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}


{% if show_confirmation|default(false) %}
    {% set attr = {'data-confirmation': 'true'} %}
    {{ include('user/deleteSubjectConfirmation.html.twig') }}
{% endif %}

{{ form_start(form, {attr: attr|default({})}) }}
    {{ form_widget(form) }}

<button type="submit" class="{{ button_css|default("btn btn-primary") }}">
    <i class="fa fa-save" aria-hidden="true"></i> {{ button_label|default('create_subject') }}
</button>

{% if include_back_to_home_link|default(false) %}
    <a href="{{ path('subject_index') }}" class="btn btn-link">
        <i class="fa fa-list-alt" aria-hidden="true"></i> {{ 'back to subject list' }}
    </a>
{% endif %}
{{ form_end(form) }}