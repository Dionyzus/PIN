{% extends 'user/layout.html.twig' %}

{% block body_id 'user_enroll' %}

{% block main %}
    <h1>{{ 'Enroll Subject' }}</h1>


    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary">
        <i class="fa fa-save" aria-hidden="true"></i> {{ 'Finish'}}
    </button>
    {{ form_end(form) }}
{% endblock %}


